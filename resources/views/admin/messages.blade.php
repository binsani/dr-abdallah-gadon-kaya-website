@extends('layouts.admin') @section('heading','Contact Messages') @section('content')<div class="list-group shadow-sm">@forelse($items as $m)<a class="list-group-item list-group-item-action p-3" href="{{route('admin.messages.show',$m)}}"><div class="d-flex justify-content-between"><strong>{{$m->is_read?'':'● '}}{{$m->subject}}</strong><small>{{$m->created_at->diffForHumans()}}</small></div><span>{{$m->name}} · {{$m->email}}</span></a>@empty<div class="p-5 text-center">No messages.</div>@endforelse</div><div class="mt-3">{{$items->links()}}</div>@endsection
