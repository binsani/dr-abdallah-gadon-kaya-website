@extends('layouts.admin') @section('heading',ucwords(str_replace('_',' ',$type))) @section('content')<div class="d-flex justify-content-end mb-3"><a class="btn btn-success" href="{{route('admin.content.create',$type)}}">Add new</a></div><div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th class="ps-4">Title</th><th>Updated</th><th class="text-end pe-4">Actions</th></tr></thead><tbody>@forelse($items as $item)<tr><td class="ps-4"><strong>{{$item->title}}</strong>@if(isset($item->status))<br><small class="text-muted">{{$item->status}}</small>@endif</td><td>{{$item->updated_at?->format('M j, Y')}}</td><td class="text-end pe-4"><a class="btn btn-sm btn-outline-secondary" href="{{route('admin.content.edit',[$type,$item->id])}}">Edit</a><form class="d-inline" method="post" action="{{route('admin.content.destroy',[$type,$item->id])}}" onsubmit="return confirm('Delete this item?')">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger">Delete</button></form></td></tr>@empty<tr><td colspan="3" class="text-center p-5 text-muted">Nothing here yet. Add the first item.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{$items->links()}}</div>@endsection
