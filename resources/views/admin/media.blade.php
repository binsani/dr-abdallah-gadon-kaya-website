@extends('layouts.admin') @section('heading','Media Library') @section('content')<form class="card p-4 border-0 shadow-sm mb-4" method="post" enctype="multipart/form-data" action="{{route('admin.media.upload')}}">@csrf<div class="row g-2"><div class="col-md-6"><input class="form-control" type="file" name="files[]" multiple required></div><div class="col-md-4"><input class="form-control" name="alt_text" placeholder="Image description (optional)"></div><div class="col-md-2"><button class="btn btn-success w-100">Upload</button></div></div></form><div class="row g-3">@foreach($items as $m)<div class="col-6 col-md-4 col-xl-3"><div class="card h-100">@if(str_starts_with($m->mime_type,'image/'))<img src="{{Storage::url($m->file_path)}}" class="card-img-top" style="aspect-ratio:1;object-fit:cover" alt="{{$m->alt_text}}">@endif<div class="card-body"><small>{{$m->name}}</small><form method="post" action="{{route('admin.media.delete',$m)}}">@csrf @method('delete')<button class="btn btn-sm btn-link text-danger p-0 mt-2">Delete</button></form></div></div></div>@endforeach</div><div class="mt-3">{{$items->links()}}</div>@endsection
