@extends('layouts.admin') @section('heading','Users & Roles') @section('content')<div class="row g-4"><div class="col-lg-5"><form class="card p-4 border-0 shadow-sm" method="post" action="{{route('admin.users.save')}}">@csrf<h2 class="h5">Add administrator</h2><input class="form-control mb-2" name="name" placeholder="Name" required><input class="form-control mb-2" type="email" name="email" placeholder="Email" required><select class="form-select mb-2" name="role"><option value="editor">Editor</option><option value="super_admin">Super Admin</option></select><input class="form-control mb-3" type="password" name="password" placeholder="Temporary password (10+ characters)" required><button class="btn btn-success">Create user</button></form></div><div class="col-lg-7"><div class="list-group shadow-sm">@foreach($items as $u)<div class="list-group-item d-flex justify-content-between align-items-center"><div><strong>{{$u->name}}</strong><br><small>{{$u->email}} · {{str_replace('_',' ',$u->role)}}</small></div>@if($u->id!==auth()->id())<form method="post" action="{{route('admin.users.delete',$u)}}" onsubmit="return confirm('Delete this account?')">@csrf @method('delete')<button class="btn btn-sm btn-outline-danger">Delete</button></form>@endif</div>@endforeach</div></div></div>@endsection
